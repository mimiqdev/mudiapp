# Persistence

- Store passwords, private keys and trusted host keys in Keychain.
- Store non-sensitive Host metadata and restoration identifiers in app-local data.
- Never write terminal contents or credentials to logs.
